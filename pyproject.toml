[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mantra-prototype"
version = "0.1.0"
dependencies = [
  "fastapi",
  "uvicorn",
  "numpy",
  "httpx",
]

[tool.setuptools.packages.find]
include = ["mantra*"]
